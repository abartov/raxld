<h1>Listing annotation_target_infos</h1>

<table>
  <tr>
    <th>Uri</th>
    <th>Mime type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @annotation_target_infos.each do |annotation_target_info| %>
  <tr>
    <td><%= annotation_target_info.uri %></td>
    <td><%= annotation_target_info.mime_type %></td>
    <td><%= link_to 'Show', annotation_target_info %></td>
    <td><%= link_to 'Edit', edit_annotation_target_info_path(annotation_target_info) %></td>
    <td><%= link_to 'Destroy', annotation_target_info, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Annotation target info', new_annotation_target_info_path %>
